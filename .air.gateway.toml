root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/gateway ./cmd/gateway"
  bin = "./tmp/gateway"
  include_ext = ["go", "toml"]
  exclude_dir = ["tmp", "bin", "vendor"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
